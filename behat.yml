default:
  suites:
    default:
      contexts:
        - FeatureContext
      paths:
        features: tests/features
  extensions:
    Behat\MinkExtension:
      base_url: 'https://dev.reservabot.es'
      goutte: ~