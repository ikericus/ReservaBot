default:
  suites:
    default:
      paths:
        - '%paths.base%/tests/features'
      contexts:
        - FeatureContext

  extensions:
    Behat\MinkExtension:
      base_url: 'https://dev.reservabot.es'
      sessions:
        default:
          goutte: ~
