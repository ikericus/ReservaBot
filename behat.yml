default:
  suites:
    default:
      contexts:
        - FeatureContext
      paths:
        features: tests/features
  extensions:
    Behat\MinkExtension:
      base_url: 'https://%env(DEV_USER)%:%env(DEV_PASSWORD)%@dev.reservabot.es' #'https://dev.reservabot.es'
      goutte: ~