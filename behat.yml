default:
  suites:
    default:
      contexts:
        - FeatureContext
      paths:
        - '%paths.base%/tests/features'
  extensions:
    Behat\MinkExtension:
      base_url: '%env(BEHAT_BASE_URL)%'
      default_session: goutte
      goutte: ~
      browser_name: chrome